<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ferllop.evermind">

    <application
        android:name=".AndroidApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Evermind">
        <activity android:name=".activities.EntryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.MainNavigationActivity" />
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.SearchCardsActivity" />
        <activity android:name=".activities.MyCards" />
        <activity android:name=".activities.MySubscriptions" />
        <activity android:name=".activities.ReviewActivity" />
        <activity android:name=".activities.ReviewResultActivity" />
        <activity android:name=".activities.CardDataActivity" />

    </application>
</manifest>